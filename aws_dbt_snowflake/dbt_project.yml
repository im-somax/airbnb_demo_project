name: 'aws_dbt_snowflake'
version: '1.0.0'
config-version: 2
profile: 'aws_dbt_snowflake'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  aws_dbt_snowflake:
    bronze:
      +materialized: table
      +schema: bronze